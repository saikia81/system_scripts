!/bin/xonsh

def list_files():
    pass

def write_file(file_name):
    pass

#load file from zip
def open_file(file_name):
    pass

def open_file_with(filename, with):
    pass


def main():
    choice_input = input("please choose an action: {}".format(str(actions.keys())))
    while choice_input.lowercase() not in ['quit','q','exit']:
        if choice_input not in actions.keys():
            break
        else:
          
        choice_input = input("please choose an action: {}".format(str(function_names)))
